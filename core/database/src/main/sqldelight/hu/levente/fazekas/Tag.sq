CREATE TABLE Tag(
    id INTEGER NOT NULL PRIMARY KEY AUTOINCREMENT,
    name TEXT NOT NULL UNIQUE
);

insert:
INSERT INTO Tag(name)
VALUES (:name);

selectAll:
SELECT * FROM Tag;

--TODO FIX DELETE
deleteById:
DELETE FROM Tag
WHERE id = :id;