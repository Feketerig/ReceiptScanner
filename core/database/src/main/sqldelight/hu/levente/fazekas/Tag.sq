CREATE TABLE Tag(
    id INTEGER NOT NULL PRIMARY KEY AUTOINCREMENT,
    name TEXT NOT NULL UNIQUE
);

insert:
INSERT OR ABORT INTO Tag(name)
VALUES (:name);

selectAll:
SELECT * FROM Tag;

deleteById:
DELETE FROM Tag
WHERE id = :id;

update:
INSERT OR REPLACE INTO Tag(id, name)
VALUES (:id, :name);