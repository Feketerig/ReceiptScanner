CREATE TABLE Tag(
    id INTEGER NOT NULL PRIMARY KEY,
    name TEXT NOT NULL UNIQUE
);

insert:
INSERT OR IGNORE INTO Tag(name)
VALUES (:name);

selectAll:
SELECT * FROM Tag;

deleteById:
DELETE FROM Tag
WHERE id = :id;

update:
UPDATE Tag
SET name = :name
WHERE id = :id;